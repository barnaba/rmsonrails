<!DOCTYPE html>
<html>
  <head>
    <title><%= t('layout.title') %> </title>
    <link href="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.2.2/css/bootstrap-combined.min.css" rel="stylesheet" />
    <link href='http://fonts.googleapis.com/css?family=Montserrat+Subrayada:400,700' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application", :media => "all" %>

    <%= javascript_include_tag "application" %>
    <script src="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.2.2/js/bootstrap.min.js"></script>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="wrap">
      <div id="header">
        <div class="row">
          <div class="span10 offset2">
            <h1> <%= t('layout.header_html') %></h1>
          </div>
        </div>
      </div>

      <div id="content">
        <div class="row" id="container">
          <div class="main-page-container offset2 span10">
            <% if notice %>
              <p id="notice" class="alert alert-success"><%= notice %></p>
            <% end %>
            <div class="row">
              <aside class="span3">
              <img src="<%= image_path "rms.jpg" %>" alt="<%= t('layout.picture_alt')%>" />
              </aside>

              <div class="span7 tabbable main-page-content">
                <nav>
                <ul class="nav nav-tabs">
                  <li><%= link_to t('layout.tabs.lecture'), root_path + "#lecture"%></li>
                  <li><%= link_to t('layout.tabs.bio'), root_path + "#bio" %></li>
                  <li><%= link_to t('layout.tabs.registration'), root_path + "#registration"%></li>
                  <li><%= link_to t('layout.tabs.stream'), root_path + "#stream"%></li>
                  <li><%= link_to t('layout.tabs.main'), root_path + "#main"%></li>
                  <li><%= link_to t('layout.tabs.media'), root_path + "#media"%></li>
                </ul>
                </nav>

                <div class="tab-content">
                  <%= yield %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
  </body>
</html>
