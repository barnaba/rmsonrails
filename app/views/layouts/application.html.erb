<!DOCTYPE html>
<html>
  <head>
    <title><%= t('layout.title') %> </title>
    <link href="http://netdna.bootstrapcdn.com/twitter-bootstrap/2.2.2/css/bootstrap-combined.min.css" rel="stylesheet" />
    <link href='http://fonts.googleapis.com/css?family=Montserrat+Subrayada:400,700' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application", :media => "all" %>

    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="wrap">
      <div id="header">
            <h1> <%= t('layout.header_html') %></h1>
      </div>

      <% if notice %>
        <p id="notice" class="alert alert-success"><%= notice %></p>
      <% end %>

      <aside></aside>
      <div id="content">

        <div class="main-page-content">
          <nav>
          <ul class="nav nav-tabs">
            <li <%=active_if('pages#info')%>><%= link_to t('layout.tabs.info'), root_path%></li>
            <li <%=active_if('attendees#new')%>><%= link_to t('layout.tabs.registration'), new_attendee_path%></li>
            <li <%=active_if('pages#streams')%>><%= link_to t('layout.tabs.stream'), streams_pages_path%></li>
            <li <%=active_if('pages#media')%>><%= link_to t('layout.tabs.media'), media_pages_path%></li>
          </ul>
          </nav>

          <div class="tab-content">
            <%= yield %>
          </div>
        </div>
        <span class="attribution">
          <%= t('layout.attribution_html')%>
      </span>
      </div>
    </div>
  </div>
</div>
  </body>
</html>
